# Part 2 dependencies, kept separate from part 1 to avoid clashes.

# Validation and pipeline (validation + tests run offline, no API key needed):
pyyaml
jsonschema
pytest

# Runner and estimate (needed to run queries and count tokens against the API):
anthropic          # Claude API + count_tokens
tiktoken           # GPT-family token counts (used only when an OpenAI model is enabled)

# Added in later milestones (judge, report):
#   openai
#   pandas
#   matplotlib
